server:
  port: 8089
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
#  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath*:mapper/*Mapper.xml
#  configuration:
#    cache-enabled: true
logging:
  level:
    cn.mybatis.mapper: debug